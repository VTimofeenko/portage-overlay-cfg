---
- name: Converge
  hosts: all
  tasks:
    - name: "Disable guru overlay"
      include_role:
        name: "gentoo-overlay"
      vars:
        gentoo_overlay_repos:
          - name: guru
            state: disabled
